- name: setup mysql server
  hosts: mysql
  become: yes
  tasks:
  - name: install mysql server
    ansible.builtin.package:
      name: mysql-server
      state: present

  - name: start and enable mysql
    ansible.builtin.package:
      name: